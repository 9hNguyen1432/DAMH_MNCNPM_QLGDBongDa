<div class="container p-100">
    <div class="row">
        <h5 class="headerBody center-horizontal">{{title}}</h5>
    </div>


    {{#if AllMatchs}}
    <div class="tab-pane fade show active" id="nav-all"
        role="tabpanel" aria-labelledby="nav-home-tab">
        {{#each AllMatchs}}
        <a style="text-decoration: none" href="/result/{{this.id}}">
            {{>renderDSTranDau this}}
            </a>
        {{/each}}
    </div>
    {{/if}}
</div>